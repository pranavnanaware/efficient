/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["utfs.io", "img.clerk.com", "subdomain", "assets.aceternity.com"],
  },
  reactStrictMode: false,
};

export default nextConfig;
